<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goalkeeper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Goalkeeper">
        <activity
            android:name=".createGoal"
            android:theme="@style/Theme.Goalkeeper.NoActionBar">></activity>
        <activity
            android:name=".createSubject"
            android:theme="@style/Theme.Goalkeeper.NoActionBar">
            >
        </activity>
        <activity
            android:name=".Register"
            android:theme="@style/Theme.Goalkeeper.NoActionBar">
            >
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/Theme.Goalkeeper.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GoalsInSubject"
            android:theme="@style/Theme.Goalkeeper.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Goalkeeper.NoActionBar" />
    </application>

</manifest>